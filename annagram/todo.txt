cigarette

for each letter in word
- if the dictionary contains a word that starts with and is less than rem_words
add to possible words dict
append next character to word

if pos_words contains ci words that are less than rem_words
else:
replace ci with cg:

cigarette

for char in anagram_word

	for word in dictionary:
		if word.startswith(char) and len(rem_words) >
